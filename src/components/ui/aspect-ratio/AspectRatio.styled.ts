'use client';

import * as AspectRatioPrimitive from '@radix-ui/react-aspect-ratio';
import styled from 'styled-components';

export const AspectRatioRoot = styled(AspectRatioPrimitive.Root)`
  width: 100%;
`;